import { L } from "ts-toolbelt";
import { Union, Error } from "json-schema-to-ts/lib/meta-types";
import { Get, DeepMergeUnsafe } from "json-schema-to-ts/lib/utils";
import { ParseSchema } from "json-schema-to-ts/lib/parse-schema";
export declare type ParseMixedSchema<S, T = Get<S, "type">> = T extends L.List ? Union<RecurseOnMixedSchema<T, S>> : Error<"Mixed schema 'type' property should be an array">;
declare type RecurseOnMixedSchema<T extends L.List, S, R = never> = {
    stop: R;
    continue: RecurseOnMixedSchema<L.Tail<T>, S, R | ParseSchema<DeepMergeUnsafe<S, {
        type: L.Head<T>;
    }>>>;
}[T extends [any, ...L.List] ? "continue" : "stop"];
export {};
