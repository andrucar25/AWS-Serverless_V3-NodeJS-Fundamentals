import { Enum, Intersection } from "json-schema-to-ts/lib/meta-types";
import { DeepGet, HasKeyIn } from "json-schema-to-ts/lib/utils";
import { ParseSchema } from "json-schema-to-ts/lib/parse-schema";
export declare type ParseEnumSchema<S> = HasKeyIn<S, "const" | "type"> extends true ? Intersection<Enum<DeepGet<S, ["enum", number]>>, ParseSchema<Omit<S, "enum">>> : Enum<DeepGet<S, ["enum", number]>>;
